# Routes

TanStack Router file-based routes live here.

Conventions: see `documentations/frontend/conventions/routing.md`.

Files in this directory follow the TanStack Router file-based convention. The
generated route tree is emitted to `app/routeTree.gen.ts` on dev/build by the
`@tanstack/router-vite-plugin` configured in `vite.config.ts`.
